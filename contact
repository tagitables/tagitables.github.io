<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Contact Page</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 20px;
            text-align: center;
        }

        .btn {
            text-decoration: none;
            color: white;
            background-color: #007bff;
            padding: 10px 20px;
            border-radius: 5px;
            font-size: 16px;
        }

        .btn:hover {
            background-color: #0056b3;
        }

        #map {
            width: 900px;
            height: 500px;
            margin: auto;
        }
    </style>
</head>

<body>
    <h2>Contact</h2>
    <p>
        <a href="mailto:quahkaixin@gmail.com" class="btn">📧 quahkaixin@gmail.com</a>
    </p>
    <p>
        <a href="tel:+1234567890" class="btn">☎️ +1234567890</a>
    </p>
    <p>
        <a href="https://www.google.com/maps/place/Singapore" class="btn">📍 Singapore</a>
    </p>
    <p>
        <a href="https://github.com/tagitables" class="btn">@tagitables</a>
    </p>

    <div id="map"></div>

    <script type="text/javascript" src="https://www.gstatic.com/charts/loader.js"></script>
    <script type="text/javascript">
        google.charts.load('current', { packages: ['geochart'] });
        google.charts.setOnLoadCallback(drawMap);

        function drawMap() {
            var data = google.visualization.arrayToDataTable([
                ['Country', 'Value'],
                ['Singapore', 1]
            ]);

            var options = {
                colorAxis: { colors: ['#e5e5e5', '#0d3378'] },
                tooltip: { trigger: 'none' },  // Hide tooltip
                backgroundColor: 'transparent' // Optional: make the background transparent
            };

            var chart = new google.visualization.GeoChart(document.getElementById('map'));
            chart.draw(data, options);
        }
    </script>
</body>

</html>
